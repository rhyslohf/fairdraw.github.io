# fairdraw.github.io
in 350 characters  11:20 A transparent random picker where names are added in sign-up order, then a winner is drawn deterministically — the same ordered list always produces the same result. Share the link and anyone can verify the outcome themselves. No server, no database, no trust required.
